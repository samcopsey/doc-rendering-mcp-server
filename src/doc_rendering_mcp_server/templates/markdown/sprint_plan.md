---
title: "Sprint Plan — {{ sprint_name }}"
team: "{{ team_name }}"
project: "{{ project_name | default('') }}"
sprint_dates: "{{ sprint_dates }}"
date_generated: "{{ generated_date | default('') }}"
author: "Cast (AI-generated)"
---

# Sprint Plan: {{ sprint_name }}

**Team:** {{ team_name }}
**Period:** {{ sprint_dates }}
{% if project_name %}**Project:** {{ project_name }}{% endif %}

---

{% if total_planned_hours or total_planned_points %}
## Sprint Overview

| Metric | Value |
|--------|-------|
{% if total_planned_hours %}| Total Planned Hours | {{ total_planned_hours }}h |
{% endif %}{% if total_planned_points %}| Total Story Points | {{ total_planned_points }} |
{% endif %}{% if recommended_commitment %}| Recommended Commitment | {{ recommended_commitment }} |
{% endif %}{% if historical_velocity %}| Historical Avg Velocity | {{ historical_velocity }} |
{% endif %}

{% endif %}
## Work Items

| Title | Type | Est. Hours | Activity | Assignee |
|-------|------|------------|----------|----------|
{% if work_items is string %}| {{ work_items }} | | | | |
{% else %}{% for item in work_items %}| {{ item.title }} | {{ item.type | default(item.work_item_type | default("")) }} | {{ item.hours | default(item.est_hours | default(item.original_estimate | default(item.estimate | default("—")))) }}h | {{ item.activity | default("—") }} | {{ item.assigned_to | default(item.assignee | default("—")) }} |
{% endfor %}{% endif %}

{% if capacity_summary and capacity_summary is not string %}
## Capacity Summary

| Team Member | Available Hours | Assigned Hours | Remaining Hours | Utilization |
|-------------|-----------------|----------------|-----------------|-------------|
{% for entry in capacity_summary %}| {{ entry.member_name | default(entry.name | default("—")) }} | {{ entry.available_hours | default(entry.available | default("—")) }} | {{ entry.assigned_hours | default(entry.assigned | default("—")) }} | {{ entry.remaining_hours | default(entry.remaining | default("—")) }} | {{ entry.utilization_pct | default(entry.utilization | default("—")) }}% |
{% endfor %}

{% elif capacity_summary is string %}
## Capacity Summary

{{ capacity_summary }}

{% endif %}
{% if estimation_notes %}
## Estimation Notes

{% if estimation_notes is string %}{{ estimation_notes }}{% else %}{% for note in estimation_notes %}- {{ note }}
{% endfor %}{% endif %}

{% endif %}
---
*Generated by Cast on {{ generated_date | default('') }}*
